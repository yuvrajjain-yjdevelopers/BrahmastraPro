[build]
  publish = "."

# Keep this explicit for Git-based deploys. Without a Functions directory,
# Netlify can publish the static site while omitting the generator endpoint.
[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404
